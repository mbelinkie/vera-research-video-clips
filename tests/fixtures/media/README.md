# Synthetic media fixture

`synthetic-4s.mp4` is generated locally from FFmpeg's color and sine sources. It contains no third-party media and is safe for deterministic tests.

Regenerate it with:

```bash
npm run fixture:media
```
